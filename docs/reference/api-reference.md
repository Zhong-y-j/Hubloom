# API 参考

> 状态：大纲（待编写正文）

## 本章要收录

- `GET /health`
- `POST /v1/chat`（SSE / AG-UI）
- `GET /v1/chat/history`（可选 `include_thought=true` 填回 `thought`）
- `POST /v1/events`、`GET /v1/events/types`
- `GET|POST /v1/im/wecom/callback`
- 其它示例站辅助接口（如 MCP status、本地换票 mock）按实际列出

每条：方法、路径、鉴权、关键字段、链到相关指南。
