# 配置项说明

> 状态：大纲（待编写正文）

## 本章要收录

对齐 `config/env.example.yaml` 分段：

- `llm` / `memory` / `rag` / `logging`
- `qdrant` / `neo4j`（长期记忆）
- `mcp` / `a2a` / `skills_*`
- `events` / `im.wecom`

每项：含义、默认、是否必填、示例。
